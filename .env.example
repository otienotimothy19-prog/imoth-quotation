# Copy this file to .env and fill in your DigitalOcean managed Postgres connection string.
# Find it in the DigitalOcean dashboard: Databases -> your cluster -> Connection Details
# (use the "Connection string" field, "Public network" tab).
DATABASE_URL=postgresql://USERNAME:PASSWORD@HOST:PORT/DATABASE?sslmode=require

PORT=3000
